<% content_for :title, t('edit.edit_book') %>

<h1><%= t('edit.edit_book') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('common.show_this_book'), @book %> |
  <%= link_to t('common.back_to_books'), books_path %>
</div>
