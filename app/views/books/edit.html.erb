<% content_for :title, t('views.common.edit.title', name: t('activerecord.models.book')) %>

<h1><%= t('views.common.edit.title', name: t('activerecord.models.book')) %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('views.common.show', name: t('activerecord.models.book')), @book %> |
  <%= link_to t('views.common.back', name: t('activerecord.models.book')), books_path %>
</div>
