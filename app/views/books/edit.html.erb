<% content_for :title, t('views.common.edit.title', name: Book.model_name.human) %>

<h1><%= t('views.common.edit.title', name: Book.model_name.human) %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('views.common.show', name: Book.model_name.human), @book %> |
  <%= link_to t('views.common.back', name: Book.model_name.human), books_path %>
</div>
