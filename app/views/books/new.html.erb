<% content_for :title, t('application.title') %>

<h1><%= t('views.common.new', name: t('activerecord.models.book')) %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('views.common.back', name: t('activerecord.models.book')), books_path %>
</div>
