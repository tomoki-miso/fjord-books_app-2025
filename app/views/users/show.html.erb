<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @user %>
</div>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(User.model_name.human.downcase)), users_path %>

  <%= button_to t('views.common.destroy', name: User.model_name.human.downcase), @user, method: :delete %>
</nav>
